bin/
obj/
.codegraph/
_bench/
*.user
.vs/
publish/
# MSIX signing keys — the public .cer ships with releases, the PFX never leaves the machine
certs/

# Astro product site (site/) build artifacts — deps + generated files
site/node_modules/
site/.astro/
site/dist/
site/src/env.d.ts
